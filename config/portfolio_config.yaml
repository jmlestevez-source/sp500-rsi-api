portfolio:
  max_positions:     15
  max_position_size: 0.12
  min_position_size: 0.03

turnover:
  max_one_sided_turnover: 0.30
  min_position_change:    0.02

screening:
  # Reducir de 100 a 60 para que los escenarios
  # sean max_positions*2 = 30, no 40
  prescreen_top_n: 60
  llm_batch_size:  10

risk:
  max_sector_weight:  0.40
  max_single_country: 0.90
